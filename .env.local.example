# GPT Image Generator - Environment Variables
# Root level environment variables for development and production

# === REQUIRED KEYS ===
OPENAI_API_KEY=sk-proj-Qm8757RFAKECHANGETHIS # https://platform.openai.com/settings -> API Keys
ANTHROPIC_API_KEY=sk-ant-api03-5GL6S4FAKECHANGETHIS
PERPLEXITY_API_KEY=pplx-g4rb8QbJFAKECHANGETHIST

# === SUPABASE CONFIGURATION ===
# Frontend environment variables (NEXT_PUBLIC_ prefix for browser access)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-side only (never expose to browser)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gpt_image_generator

# === MODEL CONFIGURATION ===
MODEL=claude-3-7-sonnet-20250219  # Recommended models: claude-3-7-sonnet-20250219, claude-3-opus-20240229
# === DEPRECATED MODEL CONFIGS ===
# PERPLEXITY_MODEL=sonar            # Use sonar-pro if available, otherwise sonar regular
# MAX_TOKENS=64000                  # Maximum tokens for model responses
# TEMPERATURE=0.2                   # Temperature for model responses (0.0-1.0) - lower = less creativity

# === OPENAI SPECIFIC ===
OPENAI_MODEL_CHAT=gpt-4o
OPENAI_MODEL_IMAGE=dall-e-3
OPENAI_MODEL_VISION=gpt-4o
OPENAI_MAX_TOKENS=4096
OPENAI_TEMPERATURE=0.7

# === IMAGE GENERATION ===
DEFAULT_IMAGE_SIZE=1024x1024
DEFAULT_IMAGE_QUALITY=standard
DEFAULT_IMAGE_STYLE=vivid
MAX_IMAGES_PER_REQUEST=4
MAX_IMAGE_PROMPT_LENGTH=1000

# === DEVELOPMENT CONFIGURATION ===
DEBUG=false                       # Enable debug logging (true/false)
LOG_LEVEL=info                    # Log level (debug, info, warn, error)
DEFAULT_SUBTASKS=5                # Default number of subtasks when expanding
DEFAULT_PRIORITY=medium           # Default priority for generated tasks (high, medium, low)
PROJECT_NAME=GPT Image Generator  # Project name for tasks.json metadata

# === NEXT.JS CONFIGURATION ===
NEXT_PORT=3000
NEXT_TURBO_ENABLED=true
NEXT_BUILD_TARGET=serverless
NEXT_OUTPUT=standalone

# === RATE LIMITING ===
RATE_LIMIT_REQUESTS_CHAT=50
RATE_LIMIT_REQUESTS_IMAGE=10
RATE_LIMIT_WINDOW=15min
RATE_LIMIT_ENABLED_PROD=true
RATE_LIMIT_ENABLED_DEV=false

# === CONTENT MODERATION ===
CONTENT_MODERATION_ENABLED=true
CONTENT_FILTER_LEVEL=medium
AUTO_MODERATE_IMAGES=true
AUTO_MODERATE_CHAT=false

# === SECURITY ===
CORS_ORIGIN_DEV=http://localhost:3000
CORS_ORIGIN_PROD=https://gpt-image-generator.vercel.app
SESSION_SECRET_LENGTH=32
JWT_EXPIRY=24h

# === PERFORMANCE & CACHING ===
CACHE_TTL_IMAGES=7d
CACHE_TTL_CONVERSATIONS=1h
CACHE_TTL_MESSAGES=30min

# === ANALYTICS & MONITORING ===
METRICS_ENABLED=true
ERROR_TRACKING_ENABLED=true
PERFORMANCE_MONITORING=true
ANALYTICS_ENABLED=true
TRACK_USER_INTERACTIONS=true
TRACK_GENERATION_METRICS=true

# === FEATURE FLAGS ===
FEATURE_IMAGE_EDITING=true
FEATURE_CONVERSATION_EXPORT=true
FEATURE_DARK_MODE=true
FEATURE_REAL_TIME_CHAT=true
FEATURE_IMAGE_VARIATIONS=true

# === TESTING ===
TEST_TIMEOUT=30000
E2E_BASE_URL=http://localhost:3000
E2E_TIMEOUT=60000
PLAYWRIGHT_WORKERS=1

# === DEVELOPMENT ENVIRONMENT ===
DEPLOYMENT_ENV=local

# === DEPLOYMENT ===
DEPLOYMENT_PLATFORM=vercel
AUTO_DEPLOY_ENABLED=true
PREVIEW_DEPLOYMENTS=true

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Replace FAKE keys with real API keys
# 3. Update Supabase URLs and keys from your project dashboard
# 4. Never commit real keys to version control
# 5. Use supabase secrets set for production deployment
